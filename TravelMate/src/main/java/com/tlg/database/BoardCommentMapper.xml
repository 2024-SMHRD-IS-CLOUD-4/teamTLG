<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BoardCommentMapper">
	<insert id="cmt_content" parameterType="com.tlg.model.BoardComment">
		insert into comment_info(cmt_idx, c_idx, cmt_content, id, created_at) values (comment_info_seq.nextval, #{c_idx}, #{cmt_content}, #{id}, sysdate)
	</insert>
	<select id="getComment" parameterType="int" resultType="com.tlg.model.BoardComment">
		select * from comment_info where c_idx = #{c_idx}
	</select>
	<select id="listComment" resultType="com.tlg.model.BoardComment">
		SELECT * FROM comment_info WHERE c_idx = #{c_idx, jdbcType=INTEGER} ORDER BY cmt_idx DESC
	</select>
	<select id="listCommentByPostId" parameterType="int" resultType="com.tlg.model.BoardComment">
    SELECT * FROM comment_info WHERE c_idx = #{c_idx} ORDER BY cmt_idx DESC
	</select>
</mapper>