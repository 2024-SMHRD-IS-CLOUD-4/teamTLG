<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="MemberMapper">
	<!-- 회원가입 -->
	<insert id="insert" parameterType="com.tlg.model.TmMember">
		insert into user_info (id, pw, name, nick, gender, email) values (#{id}, #{pw}, #{name}, #{nick}, #{gender}, #{email})
	</insert>
	<!-- 로그인 -->
	<select id="login" parameterType="com.tlg.model.TmMember" resultType="com.tlg.model.TmMember">
		select * from user_info where id = #{id} and pw = #{pw}
	</select>
	<!-- 회원정보 수정 -->
	<update id="update" parameterType="com.tlg.model.TmMember">
		update user_info set pw = #{pw}, nick = #{nick} where id = #{id}
	</update>
	<!-- 회원탈퇴 -->
	<delete id="delete">
		delete from user_info where pw = #{pw}
	</delete>
</mapper>